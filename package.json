{
  "name": "open-diabetic",
  "private": true,
  "description": "OpenDiabetic landing page — static, no dependencies",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -r index.html style.css assets dist/ && echo built"
  }
}
