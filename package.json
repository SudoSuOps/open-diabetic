{
  "name": "open-diabetic",
  "private": true,
  "description": "OpenDiabetic landing page — static, no dependencies",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -r index.html open.html study.html try.html style.css assets robots.txt sitemap.xml llms.txt dist/ && echo built"
  }
}
